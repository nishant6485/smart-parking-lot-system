import React from 'react';
import ReactDOM from 'react-dom/client';
import ParkingLotSystem from './parking-lot-system.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ParkingLotSystem />
  </React.StrictMode>
);
